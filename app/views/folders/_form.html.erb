<%= form_for @folder, :html => { :class => "form-horizontal folder" } do |f| %>

    <% if @folder.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@folder.errors.count, "error") %> prohibited this folder from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @folder.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>
  <div class="row">
<div class="col-md-3">
</div>
<div class="col-md-6">
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name , :class => 'form-control', :placeholder => "Jhon snow" %>
      <%= f.hidden_field :user_id , :value => "#{current_user.id}" %>
    </div>
    <%= error_span(@folder[:name]) %>
  </div>
<hr>

 <%= f.submit "Create Life", :class => 'btn btn-primary' %>
  </div>
 <div class="col-md-3">
 </div>
 </div>
<% end %>
